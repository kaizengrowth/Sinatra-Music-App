<% @messages.each do |message| %>
  <div class="message">
    <h3><a href="/messages/<%= message.id %>"><%= message.title %></a> <small>(by: <%= message.author %>)</small></h3>
    <p>
      <%= message.url %>
    </p>
    <br>
    <p>
      <a href="/messages/new">Post a song!</a>
    </p>
  </div>
    <br>
    <br>
<% end %>
